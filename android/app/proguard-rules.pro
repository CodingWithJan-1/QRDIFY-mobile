# Flutter and its plugins provide their own consumer rules. Keep runtime
# annotation and generic metadata used by platform plugins and HTTP libraries.
-keepattributes RuntimeVisibleAnnotations,RuntimeInvisibleAnnotations,Signature
